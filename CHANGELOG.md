# Changelog

## 0.1.1 - 2026-08-23

- Insert composer search inside DSH's nested scroll container.
- Fall back to provider and visible-title mapping when directory and DOM counts drift.
- Aggregate duplicate raw model IDs behind one visible row and leave only unmappable rows visible during filtering.
- Prevent handled keyboard navigation from also reaching official parent handlers.
- Show a localized no-results state in the enhanced `/model` popup.

## 0.1.0 - 2026-08-23

- Search composer models by provider, display name, or raw model ID.
- Search `/model` options without replacing official selection handlers.
- Add Chinese and English UI copy, keyboard navigation, cleanup, and safe compatibility fallback.
