export const name = "dsh-model-search";

// The host half is intentionally inert. All enhancement work runs in the
// browser and degrades independently of DSH's server startup.
export function apply() {}
